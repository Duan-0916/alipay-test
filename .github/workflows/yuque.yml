name: Publish to Preview Knowledge Base

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  publish_to_preview:
    runs-on: ubuntu-latest

    steps:
      - name: Publish to Preview Knowledge Base
        uses: Duan-0916/sofadoc@master
        with:
          yuqueNamespace: eg6z1a/qpbul9
          yuqueSite: https://mosn-layotto.yuque.com/
